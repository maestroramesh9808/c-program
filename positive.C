#include(stdio.h)
#include(conio.h)
int main()
{
printf(Enter the number);
scanf("%d", & number);
if (number < = 0.0)
{
if (number ==0.0)
printf("You have enter 0 ");
else
printf(" your number is pasitive");
}
else
printf("your number negative");
return 0:
}
